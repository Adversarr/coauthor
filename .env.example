# Seed Environment Configuration
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to git!

# LLM Provider
SEED_LLM_PROVIDER=fake

# Unified API credentials for OpenAI-compatible providers
# Required when provider is openai|bailian|volcengine
SEED_LLM_API_KEY=

# Optional: Custom provider base URL
# SEED_LLM_BASE_URL=https://api.openai.com/v1

# Canonical profile + policy catalog (JSON)
# Built-in profiles fast|writer|reasoning are required.
# Custom profiles are allowed.
# You can set this to an inline JSON string OR a JSON file path
# (absolute path, or relative to workspace dir).
# SEED_LLM_PROFILES_JSON={"defaultProfile":"fast","clientPolicies":{"default":{"openaiCompat":{"enableThinking":true}}},"profiles":{"fast":{"model":"gpt-4o-mini","clientPolicy":"default"},"writer":{"model":"gpt-4o","clientPolicy":"default"},"reasoning":{"model":"gpt-4o","clientPolicy":"default"}}}
